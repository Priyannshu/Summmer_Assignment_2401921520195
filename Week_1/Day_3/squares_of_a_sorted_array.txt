class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        int len = nums.size();
        for(int i=0;i<len;i++){
            int num = nums[i];
            nums[i] = num*num;
        }
        sort(nums.begin(),nums.end());
        return nums;
    }
};